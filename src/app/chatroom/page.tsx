import { Box } from "@mui/material";
import { Chatroom } from "../components";

const ChatroomPage = () => {
  return (
    <Box component={"main"}>
      <Chatroom />
    </Box>
  );
};

export default ChatroomPage;
